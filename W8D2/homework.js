function madLib(verb, adject, noun) {
    return "We shall ${verb.toUpperCase()} the ${adject.toUpperCase()} ${noun.toUpperCase()}"
}

function isSubstring(phrase, subphrase) {
  return phrase.includes(subphrase);
}